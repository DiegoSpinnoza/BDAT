name: CI Pipeline

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  # TESTS COMMENTED OUT - Not part of validation
  # backend-tests:
  #   name: Backend Tests (Dockerized)
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4

  #   - name: Set up Docker Buildx
  #     uses: docker/setup-buildx-action@v3

  #   - name: Create test environment file
  #     run: |
  #       cat > .env.test << EOF
  #       MYSQL_HOST=mysql
  #       MYSQL_PORT=3306
  #       MYSQL_USER=UserBDAT
  #       MYSQL_PASSWORD=BDATpassword
  #       MYSQL_DB=BDAT_FE_simulations_test
  #       FLASK_ENV=testing
  #       CORS_HEADERS=Content-Type
  #       EOF

  #   - name: Build Docker images
  #     run: |
  #       docker compose build mysql backend

  #   - name: Start services for testing
  #     run: |
  #       # Start MySQL
  #       docker compose up -d mysql
  #       echo "⏳ Waiting for MySQL to be ready..."
  #       timeout 60 bash -c 'until docker compose exec -T mysql mysqladmin ping -h localhost --silent; do sleep 2; done'

  #       # Start backend
  #       docker compose up -d backend
  #       echo "⏳ Waiting for backend to be ready..."
  #       # Retry loop for backend healthcheck
  #       for i in {1..60}; do
  #         if curl -s http://localhost:5000/health > /dev/null; then
  #           echo "✅ Backend is ready!"
  #           break
  #         fi
  #         sleep 2
  #       done

  #   - name: Run backend integration tests
  #     run: |
  #       echo "🧪 Running integration tests from host..."
  #       python -m pip install --upgrade pip
  #       pip install pytest requests python-dotenv
  #       cd backend
  #       python -m pytest test/ -s -v

  #   - name: Show service logs on failure
  #     if: failure()
  #     run: |
  #       echo "=== MySQL Logs ==="
  #       docker compose logs mysql
  #       echo "=== Backend Logs ==="
  #       docker compose logs backend

  #   - name: Clean up
  #     if: always()
  #     run: |
  #       docker compose down -v


  docker-build:
    name: Docker Build Validation
    runs-on: ubuntu-latest
    # needs: [backend-tests]  # Commented out - no longer depends on tests

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Validate Docker Compose configuration
      run: |
        docker compose config
        echo "✅ Docker Compose configuration is valid"

    - name: Build all services (cache only)
      run: |
        docker compose build --parallel
        echo "✅ All Docker images built successfully"
